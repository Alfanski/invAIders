# PR Review Patterns

Repo-specific review patterns discovered through PR reviews. The `pr-review`
and `resolve-pr-feedback` skills read this file for context.

## Quality Gates

<!-- Repo-specific non-negotiables for PRs. Examples:
- All public functions must have type annotations
- Tests must cover new/changed code paths
- CLAUDE.md/AGENTS.md must be updated if architecture changed
-->

## Recurring Findings

<!-- Patterns that keep appearing in reviews. Examples:
- Missing error handling on HTTP calls
- Inconsistent naming conventions in [module]
- Forgotten index on frequently-queried column
-->

## Anti-Patterns

<!-- Code patterns that PRs should never introduce. Examples:
- f-strings in log calls (use %s formatting)
- Bare except: clauses
- Hardcoded URLs or credentials
-->
